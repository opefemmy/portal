- [Production payments.fee_type is ENUM, not varchar](production-payments-fee-type-enum.md) — The repo migration says varchar(30), production is ENUM('application','acceptance','school_fees','hostel','library','other'). Always go through ApplicantPaymentService::feeTypeFor() to map purposes to enum-safe values.
- [Production payment_types.purpose ENUM has 8 values](production-payment-types-purpose-enum.md) — Production ENUM is (application, acceptance, school_fees, hostel, library, registration, other, compulsory). Codebase constant uses school_fee (no s); controller alias-maps. Call PaymentType::allowedPurposes() instead of hard-coding.
- [Dashboard Compulsory badge must use PURPOSE_COMPULSORY](dashboard-compulsory-purpose-coupling.md) — The applicant dashboard's Compulsory Fee badge reads applicants.compulsory_paid_at via hasPaid(PURPOSE_COMPULSORY). Don't switch it to PURPOSE_SCHOOL_FEE — that's the audience=student returning-student fee.
- [Production payments.installment is ENUM, uppercase only](production-payments-installment-enum.md) — payments.installment is ENUM('First','Second','Full'). Student controller must NOT write the lowercase SchoolFeeCalculator label there; use installment_label (varchar 20) instead.
- [markCompleted must always call applyApplicantSideEffects](markcompleted-idempotency-guard.md) — Don't early-return on status==='completed' in ApplicantPaymentService::markCompleted. The test handler's fallback path pre-creates the row 'completed' and relies on the side effects running.
- [Gateway controllers must catch \Throwable, not \Exception](gateway-empty-body-throwable.md) — json_decode() returning null + reading ->status throws \Error under PHP 8. Inner `catch (\Exception)` lets it escape to the outer generic flash.
- [DataTables skips empty-state tables with colspan mismatches](datatables-empty-state-column-count.md) — Layout auto-init guards on first-row tdCount < header thCount. Without it, the empty `@forelse/@empty` row triggers tn/18 "Incorrect column count" warning.
- [Test env defaults to MySQL despite phpunit.xml](test-env-uses-mysql-by-default.md) — `.env` shadows phpunit.xml's sqlite in-memory; fix is `.env.testing` + `php artisan config:clear`. Tests that build their own schema must include `system_settings` and `payment_gateways`.
- [Local DB drift — external_payments table missing](local-db-external-payments-drift.md) — `database_backup_20260724.sql` predates 2026_07_23_000001 so the `external_payments` table never landed locally. Run `php artisan migrate` — the new 2026_08_09_000001 safety-net creates it without touching production. PaymentSyncController is now `Schema::hasTable`-guarded so it never 500s on a missing table.
- [Local DB drift — hospital_patients deleted_at missing](local-db-hospital-soft-deletes-drift.md) — Backup predates 2024_07_07_000001/003 so `deleted_at` never landed on `hospital_patients` (or any other table those migrations touched) even though `migrate:status` shows them Ran. Safety-net migration 2026_08_09_000002 adds them locally; production is a no-op.
- [Bursar payment-tally bug — verified vs completed status split](bursar-payments-status-tally-fix.md) — Dashboard / paid-students / reports filtered `status='verified'` but PaymentController::verify and the payment flows write `'completed'`. Now all bursar queries use `whereIn('status', ['completed', 'verified'])` so totals reconcile with /bursar/payments.
- [Bursar debtor count tally — three different debtor definitions](bursar-debtor-count-tally-fix.md) — Dashboard tile / debtors list used "no payment in session" while reports used "any required fee unpaid". Single canonical `debtorQuery()` helper on `Bursar\DashboardController` powers both. Uses NOT EXISTS (NOT IN miscounts NULL-fee payments).
- [ICT admin + bursary/library/audit sub-roles need explicit route + login wiring](ict-admin-bursar-staff-route-access.md) — Seeder has them but route middleware + login redirect maps didn't. Without this, ICT admin hits 404 on /admin/dashboard and bursary staff get stuck in /dashboard loops.
- [Local DB drift — 13 hospital tables missing](local-db-broader-hospital-tables-drift.md) — `2026_08_07_000005_006` ran but never created the clinical/prescription/lab/admission tables. Migration 2026_08_11_000001 now safety-nets all 13 with `Schema::hasTable` guards; append new missing tables to it instead of creating another migration.
- [Dashboard pharmacy was querying `quantity`, real column is `current_stock`](dashboard-pharmacy-quantity-vs-current-stock.md) — `DashboardController::pharmacyDashboard` had `where('quantity','<=',10)` which 500s because `hospital_drugs` uses `current_stock` + `reorder_level`.
- [Dashboard widget foundation slice](dashboard-widget-foundation-slice.md) — After 2ebe8529 the WidgetRegistry + DashboardResolver are consumable from any audience. Shared `widgets.render` partial + `stat-card` `cta` extension + per-audience `register{...}Widgets()` methods. Pattern for migrating lecturer/HOD/dean/librarian/hospital/finance.
- [Librarian books.status doesn't exist — use available > 0](librarian-books-status-bug.md) — `books` table has `available` (int) + `is_active` (bool) but NO `status`. Pre-existing `Book::where('status','available')` 500s. Fixed in both Librarian/DashboardController and `librarian.available_books` widget closure.
- [Catalogue expansion to 8 domains](permissionservice-catalogue-expansion.md) — After 0e6ac44f the permissions table holds 202 rows across 8 groups (hospital/bursar/registrar/librarian/finance/academic/auditor/executive). `PermissionCatalog::all()` is the single source. `RolePermissionsSeeder` unions per role slug across all classes.
- [Over-broad grant audit findings](over-broad-grant-audit.md) — ict_admin is in /bursar/* + /finance/* role: lists but has 0 slugs there. Either grant the slugs (intent per routes/finance.php:13-16) or remove from role: lists. Decide before next 8i slice.
- [Run seeders after every catalogue change](seeder-required-after-permission-constants.md) — After editing *Permissions::ROLE_PERMISSIONS, run PermissionsSeeder + RolePermissionsSeeder on live DB or every ict_admin/staff/etc. user 403s at endpoints they should reach. Idempotent but RolePermissionsSeeder sync() overwrites admin-UI-granted perms — confirm before running.
- [Slice 8e — every non-hospital controller now gates](permissionservice-slice-8e-gating.md) — Commit 9b5f3e9e. 38 non-hospital controllers got `requirePermission()`; trait renamed `EnforcesHospitalPermission` → `EnforcesPermission`. Total calls 150 → 268. New `BusinessCommitteePermissions` + `academic.results.board-approve` slug. Wildcard `*` is honoured ONLY for roles in `HospitalPermissions::ROLE_PERMISSIONS` — other roles need explicit pivot rows.
- [8i-admin-facilities sub-slice 6 — 2 pre-existing route-setup issues](8i-admin-facilities-subslice6-issues.md) — HostelController (15 gates) + LibraryController (7 gates) wired. (1) `/admin/hostels/allocations` shadowed by resource show route → use `/allocations/create` in tests. (2) `library.access` middleware needs `settings` table in test schema → add it in `buildSchema()`.
